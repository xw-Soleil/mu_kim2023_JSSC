# 28nm 首版签核报告 — PT post-route STA + Calibre DRC/LVS/ANT(2026-08-07)

签核对象:`pnr28-first-clean`(commit 08d19f1)的 P&R 产物,未做任何设计修改。
这是本项目(含 65nm 两条线)第一次到达独立签核环节:PT 首跑、Calibre LVS 首跑。
执行机:EDAServer(PT O-2018.06-SP1,Calibre v2015.2_36.27,40 核)。
归档:`flow/local_runs/icc2_28nm_20260807/signoff/`(本地权威副本 + MANIFEST.md,
含全部 EDAServer 绝对路径、deck 版本与逐文件 sha256)。

---

## 第一部分 传输与环境(Stage F)

- 15 个输入文件(netlist / WC·BC SPEF / SDC / GDS / SDF / NLDM×2 / stdcell spice /
  DRC·ANT·LVS deck / gdsout map,约 1.0 GB)由 soleilUbuntu 传输至 EDAServer 签核
  工作树,**两端 sha256 逐一核对全部一致**(清单见 MANIFEST.md §1)。
- EDAServer /home 分区剩余 239 GB,满足。
- PT license checkout 正常(SCL 27000@eda UP);该机无 soleilUbuntu 的 libudev 段错
  故障,未使用 snps_no_udev 包装。pt_shell 启动时有一条 `PT-063 Library Compiler
  executable path is not set`,与 .db 读取无关,全程无影响,如实记录。
- EDAServer 上存在未核验的 tsmc28 PDK 目录;签核必须与实现同库,未使用,一律
  使用传输过去的 hash 核对子集。
- 约束采用 P&R 实际读入的那份 SDC(DC28 输出);其中已含
  `set_clock_uncertainty -setup 0.2 / -hold 0.05`,与 ICC2 s01 的 in-flow 设置数值
  一致,PT 侧无需额外附加。SDC 仍带 DC 时代的 `set_ideal_network [get_ports clk]`,
  PT 中按 ICC2 CTS 后实际状态执行 `remove_ideal_network` + `set_propagated_clock`
  ——引入真实时钟树延迟,属加严,非放宽。

## 第二部分 PrimeTime post-route STA(Stage G)

MMMC 与 ICC2 对齐:WC = ssg0p9vm40c(0.9 V/−40 °C)+ rcworst SPEF,做 setup/
recovery/DRV;BC = ffg0p99vm40c(0.99 V/−40 °C)+ rcbest SPEF,做 hold/removal。
分析 OCV + CRPR(`timing_remove_clock_reconvergence_pessimism true`),无 derate。
**AOCV 本次未启用**(基线与 ICC2 可比);sbocv .aocvm 接入点已注释在
`flow/signoff28/pt_sta28.tcl`(read_aocvm + set_timing_derate -aocvm)。

SPEF 标注(report_annotated_parasitics -check,两角相同):有驱动网络
142,608(内部)+ 57(边界)= **142,665 个,100% RC network 标注**;另有 20,513 个
driverless nets 未标注(无驱动端网络,来源未逐一定性,如实记录)。

### G.3 recovery/removal 启用证据(实测,非假设)

- PT 变量 `timing_disable_recovery_removal_checks` **默认即 false**(日志原文:
  `PDE28_PT pre-set timing_disable_recovery_removal_checks = false`),脚本仍显式
  置 false 使状态入档。
- removal 报告返回真实路径(BC_removal_paths.rpt 首条):
  Startpoint `rst_sync_q_reg_1_` → Endpoint
  `u_impl/u_core/g_row_7__g_col_1__u_pe/u_status/small_q_reg`
  `(removal check against rising-edge clock core_clk)`,含 `library removal time`
  行——不是 No paths。

### G.4 结果与 ICC2 correlation

| 检查类 | 受检数 | 违例 | PT WNS | ICC2(s07) | Δ |
|---|---|---|---|---|---|
| setup(WC) | 23,907 | 0 | +5.23 | +5.25 | 0.02 |
| hold(BC) | 23,907 | 0 | +0.02 | 0.00 | 0.02 |
| recovery(WC) | 16,227 | 0 | +7.93 | +7.96 | 0.03 |
| removal(BC) | 16,227 | 0 | +0.17 | +0.17 | 0.00 |

TNS 四类均 0,违例端点数 0(get_timing_paths 全端点扫描:
`WC_max WNS=0 TNS=0 NVP=0`,`BC_min WNS=0 TNS=0 NVP=0`)。
min_pulse_width:160,536 项,violated 0(untested 64,910,未逐项归因,见第六部分)。

untested 端点全部有下落(6 个,report_analysis_coverage -status_details):
`rst_sync_q_reg_0_/D` setup+hold ×2(D 端 tie 常量,constant_disabled);
`rst_sync_q_reg_0_/CDN`、`rst_sync_q_reg_1_/CDN` recovery+removal ×4(rst_n 端口
false_path,同步器输入侧异步是设计意图);下游 16,225 个 CDN 全部受检。

### DRV(PT 视角,ICC2 对照)——本次签核的真发现

| 项 | PT WC | PT BC | ICC2 s07 | 说明 |
|---|---|---|---|---|
| max_transition | **1,101 pin,最差 −0.03**(限 0.50) | 0 | 19 个,−0.01 | PT 延迟计算与 RC-009 驱动电阻修正下更悲观;数量差异如实报告,未调参靠拢 |
| max_capacitance | 6 pin,数值四舍五入为 0.00 | 1 pin | 6(constraints)/7(QoR) | 即 ICC2 已知的边缘项 |
| max_fanout | **1,110 net,最差 −253**(fanout 285 vs 限 32) | 同 1,110 | 未报告(无此段) | 428 个为 ZCTS* 时钟树单元——ICC2 的 DRC 默认豁免时钟网络,PT 不豁免;约 662 个为 HFSBUF/HFSINV(fanout 40);其余零星 |

结论:时序四类双工具一致(≤0.03 ns);DRV 是规则口径差异 + 实现遗留的混合,
清理策略见第五部分。

## 第三部分 Calibre 物理验证(Stage H)

### 冒烟(H.2)

DRC deck 为 SVRFencrypt **v2016.4** 加密,执行工具为 Calibre **2015.2**(低于 deck
QA 版本,偏差记录于 MANIFEST §2)。QA 样片冒烟:解密并执行成功,2,680 条规则、
2 秒完成——加密兼容风险排除后才跑全设计。

### 精度配置编辑(用户 2026-08-07 批准)

首跑失败:GDS 数据库精度 0.0001 µm(10000 dbu/µm,ICC2 写出)与 deck 头
`PRECISION 1000` 不一致(Calibre 原文:`Rule file precision 1000 is not consistent
with database precision 10000`)。经用户批准后仅在 **runset 工作副本**编辑,理由
(用户原文四条):① 物理格点同为 5 nm,规则尺寸值(µm)不变,非放宽;② TSMC
LVS deck 官方 HIGH_RESOLUTION 开关即 PRECISION 10000,该精度为原厂认可配置;
③ 编辑位于 deck 自带 SWITCH DEFINITION 用户配置区;④ 原始 deck 不动。

TSMC 官方依据原文(LVS deck v1.0_3o 第 844、937–944 行,逐字):

```
#define HIGH_RESOLUTION	// uncomment this line to set resolution as 0.1nm, comment this line to set resolution as 1nm
...
#IFDEF HIGH_RESOLUTION
LAYOUT PRECISION 1000
PRECISION 10000
RESOLUTION 1
LAYOUT MAGNIFY AUTO 
#ELSE
PRECISION 1000
RESOLUTION 1
#ENDIF
```

三个 runset 工作副本相对原件的逐字 diff(含精度行、LAYOUT/SOURCE 占位符与
工艺开关;运行路径为 EDAServer 签核工作树):

DRC(deck_pde28.svrf vs CLN28HP_9M_4X2Y2R_002.18a.encrypt):

```
48c48
< //#DEFINE N28_HPC_PLUS                  // Turn on for HPC+ process; Turn off for HP process
---
> #DEFINE N28_HPC_PLUS                  // Turn on for HPC+ process; Turn off for HP process
54,55c54,55
< #DEFINE FULL_CHIP                       // Turn on for chip level design
< #DEFINE WITH_SEALRING                   // Turn on if Seal-Ring is already added in chip layout 
---
> //#DEFINE FULL_CHIP                       // Turn on for chip level design
> //#DEFINE WITH_SEALRING                   // Turn on if Seal-Ring is already added in chip layout 
57,58c57,58
< #DEFINE WITH_APRDL                      // Turn on if with AlCu RDL (FBEOL option3/4); Turn off if without AlCu RDL (FBEOL option1/2)
< #DEFINE WITH_POLYIMIDE                  // Turn off when process without Polyimide
---
> //#DEFINE WITH_APRDL                      // Turn on if with AlCu RDL (FBEOL option3/4); Turn off if without AlCu RDL (FBEOL option1/2)
> //#DEFINE WITH_POLYIMIDE                  // Turn off when process without Polyimide
320,321c320,321
< PRECISION    1000
< RESOLUTION      5    // For 0.005um layout grid  
---
> PRECISION    10000
> RESOLUTION      50    // For 0.005um layout grid  
324,325c324,325
< LAYOUT PATH "GDSFILENAME"
< LAYOUT PRIMARY "TOPCELLNAME"
---
> LAYOUT PATH "/home/sxw/PDE/pde28_signoff/20260807/inputs/pde_chip_top_safe.gds"
> LAYOUT PRIMARY "pde_chip_top_safe"
```

ANT(ant_pde28.svrf vs CLN28HP_9M.ANT_002.18a):

```
29,30c29,30
< PRECISION    1000
< RESOLUTION      5
---
> PRECISION    10000
> RESOLUTION      50
33,34c33,34
< LAYOUT PATH "GDSFILENAME"
< LAYOUT PRIMARY "TOPCELLNAME"
---
> LAYOUT PATH "/home/sxw/PDE/pde28_signoff/20260807/inputs/pde_chip_top_safe.gds"
> LAYOUT PRIMARY "pde_chip_top_safe"
```

LVS(deck_lvs_pde28.svrf vs 安装器生成件;HPC_PLUS_PROCESS 依据为 TSMC LVS
README 原文 "For N28HPC_PLUS process, please turn on HPC_PLUS_PROCESS switch or
choose N28HPC_PLUS during installation"——安装器命令行无该 flag,开关是官方指定
路径;本设计库为 tcbn28**hpcplus**bwp40p140):

```
837c837
< #define HPP_PROCESS		// uncomment this line when the process is N28HP+
---
> //#define HPP_PROCESS		// uncomment this line when the process is N28HP+
839c839
< //#define HPC_PLUS_PROCESS	// uncomment this line when the process is N28HPC+
---
> #define HPC_PLUS_PROCESS	// uncomment this line when the process is N28HPC+
905,906c905,906
< LAYOUT PRIMARY "lvs_top"
< LAYOUT PATH "lvs_top.gds"
---
> LAYOUT PRIMARY "pde_chip_top_safe"
> LAYOUT PATH "/home/sxw/PDE/pde28_signoff/20260807/inputs/pde_chip_top_safe.gds"
911,912c911,912
< SOURCE PRIMARY "lvs_top"
< SOURCE PATH "lvs_top.cdl"
---
> SOURCE PRIMARY "pde_chip_top_safe"
> SOURCE PATH "/home/sxw/PDE/pde28_signoff/20260807/calibre/lvs/pde_chip_top_safe.src.spi"
938c938
< LAYOUT PRECISION 1000
---
> LAYOUT PRECISION 10000
```

### H.3 DRC(deck CLN28HP_9M_4X2Y2R_002.18a,块级配置)

全设计:2,415 条规则执行,632 s(8 CPU)。结果分类(总 1,307 项 / 原始 206,934):

| 类别 | 规则与计数 | 定性 |
|---|---|---|
| **LUP(latch-up)族** | **22 条规则全部 0**(LUP.1、LUP.2、LUP.3.x×8、LUP.4、LUP.5.x×8、LUP.6、LUP.10__LUP.13、LUP.14) | **tap/endcap 插入的最终裁决:通过**。60 µm tap 间距方案经 foundry deck 确认 |
| 密度族(DN) | 1,033 项:M2/M3/M4/M5.DN.6=307/163/273/254,M2–M9.DN.1 共 20,OD.DN.3=7,PO.DN.3(.1)=5,Mx.DN.7=8 | 未做 dummy fill(OD/PO/金属)所致,系流片准备步骤缺失,非布线/单元错误 |
| OD.S.14 | 68 项(规则:任意点 10 µm 内须有 OD/dummy OD;结果均为 die 边缘与 core 之间空白带,样本 x≈5.0–5.2 µm 竖条) | 与密度族同根:无 dummy OD |
| warning 类 | IO_CONNECT_CORE_NET_VOLTAGE_IS_CORE:WARNING1 = 201(原始 205,828;无电压标记文本时按 core 电压处理的提示)、DIODMY_L:WARNING = 1 | 提示类,非制造规则违例 |
| NOT EXECUTED | HVD_P.S.15、HVD_N.S.15 | deck 内部条件未触发,原因未查明(第六部分) |
| **其余全部规则** | **0** | 全部间距/宽度/包络/via/井规则干净 |

DRC 判读:**制造规则层面,除"未做 dummy fill"一项系统性缺失外全零**;
ICC2 check_routes DRC=0 与 Calibre 在布线类规则上的结论一致(fill 与密度不在
ICC2 检查范围,不构成矛盾)。块级开关(FULL_CHIP/SEALRING/APRDL/POLYIMIDE 关)
意味着流片前必须以全芯片配置(含 sealring、封装选项)重跑。

### H.5 Antenna(deck CLN28HP_9M.ANT_002.18a,明文)

109 条规则(A.R.1–A.R.9 全金属层/via 层 + galvanic),**全部 0**。
与 ICC2 流程内天线违例 0、二极管插入 0 一致——**独立裁决未发现覆盖缺口**。

### H.4 LVS(deck T-N28-CL-LS-002-C1 v1.0_3o——本项目首次 LVS)

来源网表:v2lvs v2015.2(`-v postroute.v -lsp/-s tcbn28hpcplusbwp40p140_110a.spi
-s0 VSS -s1 VDD`);v2lvs 告警:pde_tcu 模块存在大小写仅异的网名对
(n172/N172 类),deck 侧 CASE 敏感为 YES,记录在案。

**判定:INCORRECT(as-run)**。差异完整分解(证据链齐全):

1. **晶体管级 100% 匹配**:MN(nch_mac)772,988 / MP(pch_mac)773,038,两侧全
   配对,0 unmatched。网络 798,117 对匹配,仅 2+2 不匹配(见 3)。
2. **布局侧端口 0 个 vs 源侧 59 个**(57 信号 + VDD/VSS)。根因已定位:ICC2 经
   gdsout map 写出的顶层端口 text 落在 33;0 / 34;0(信号,即 M3/M4 drawing 层
   texttype 0)与 39;80(VDD/VSS);而 deck 只认 131–139(MxTXT)或 127;3x
   (GDS TEXT 记录逐条扫描 + deck TEXT LAYER 语句比对,双向证实;标准单元内部
   text 恰在 131;0/132;0,佐证库 view 用的是 TSMC text 约定,仅顶层端口走了
   PRTF map 的另一套)。VDD/VSS 因此无名,连带电源网无法配对。
3. **cfg_addr[3]/[5] 两网差异 = 端口缺名导致的拓扑歧义伪影,非物理缺陷**。
   证据链:唯一涉事实例 u_impl/U687 为 OAI31D0(A1/A2/A3 为可交换输入);DEF
   实测 A1=cfg_addr[5]、A2=cfg_addr[3]、A3=cfg_addr[4],与网表逐字一致(无引脚
   交换、无绕线交叉);两个端口网除对称输入外拓扑不可分,Calibre 自报
   "Ambiguity points were found and resolved arbitrarily";ICC2 check_lvs
   (对其网表)开路 0 短路 0。端口 text 命名恢复后该伪影应消失(待验证)。
4. **66,132 个源侧实例 unmatched** = FILL×61,182 + TAPCELL×3,960 +
   BOUNDARY×990。库 spice 中这些 subckt 为空(`.subckt … VDD VSS` / `.ends`,
   无器件),Calibre 将空 subckt 视为待配对 box,而布局侧其几何已并入井/电源轨、
   不产生实例。属账目性差异,不涉及任何器件。
5. 信息项:2,020 个源网 deep-short 合并为 417(井/衬底连通所致)。

**修复第二轮未执行**:准备在 LVS runset 副本追加三条 text 层映射(仿 deck 自身
127;3x 写法,将 33;0/34;0/39;80 声明为 M3/M4/M9 text)后重跑,被权限门拦截
(超出精度批准范围),按既定规则停止,转为第五部分决策项 ①。

## 第四部分 签核就绪度判定

| 项 | 判定 | 依据 |
|---|---|---|
| STA setup/hold/recovery/removal | **通过** | PT 独立复核 0 违例,与 ICC2 偏差 ≤0.03 ns,removal 实路径证据在档 |
| STA DRV | **未达签核清洁** | max_transition 1,101 pin(−0.03)、max_fanout 1,110 net(时钟树豁免口径 + HFS=40);非功能风险但须清理或定约束口径 |
| DRC 制造规则 | **有条件通过** | 除 dummy fill 缺失(密度族 1,033 + OD.S.14 68)外全零;**本版不可直接流片**,fill 后须以全芯片开关重验 |
| DRC LUP 族 | **通过** | 22 条全零,tap/endcap 方案获 foundry deck 最终确认 |
| Antenna | **通过** | Calibre 独立 109 条全零,与 ICC2 一致 |
| LVS | **未通过(as-run INCORRECT)** | 器件级 100% 匹配、2 网差异已定性为端口缺名伪影,但端口 text 识别与空物理单元账目未闭环,不能宣布 CLEAN |

总评(不含糊):本版达到 **"实现质量已被独立验证"**(时序、latch-up、天线、
布线几何全部通过),**未达到"可流片"**:缺 dummy fill、LVS 未闭环、DRV 未清、
全芯片级 DRC(sealring/封装开关)未跑。

## 第五部分 需要决策的事项

1. **LVS 第二轮**:批准在 LVS runset 副本追加 33;0/34;0/39;80 → M3/M4/M9 的
   text 映射(三条语句,仿 deck 自身写法)并重跑;空物理单元差异是否同轮以
   deck FILTER 处理,还是留待下轮 P&R 从源头解决,一并裁决。
2. **gdsout text 层归位**(根治):下轮 write_gds 改用/修订 map,使顶层端口
   text 直接落在 TSMC 约定层(131–139 或 127;3x),LVS 不再依赖 runset 补丁。
3. **dummy fill 插入**纳入下轮流程(OD/PO/金属),之后密度族与 OD.S.14 重验。
4. **max_fanout 约束口径**:时钟树是否豁免(ICC2 口径)、HFS 目标 40 与 SDC
   限值 32 的矛盾,须定签核标准后再清理。
5. **max_transition 清理**与既有决策项"4–5 ns 时钟重定时"合并处理(PT 视角
   1,101 pin,ICC2 视角 19 pin,两口径已入档)。
6. **AOCV/sbocv 附加**:PT 脚本接入点已留,是否在下一轮 STA 启用。
7. Calibre 版本偏差(2015.2 < deck QA 2016.4/2015.4):是否接受为本地签核基线,
   或寻求版本对齐。

## 第六部分 失败/未完成项及原因

- `report_global_timing` 在 PT O-2018.06-SP1 上报错不可用;以
  report_analysis_coverage + get_timing_paths 全端点扫描替代(方法在脚本中)。
- LVS 修复第二轮被权限门拦截未执行(见第三、五部分),as-run 结果即本次记录。
- HVD_P.S.15 / HVD_N.S.15 显示 NOT EXECUTED,deck 内部条件未查明。
- min_pulse_width 64,910 项 untested 未逐项归因(量大,coverage 汇总在档)。
- 20,513 个 driverless nets 的来源未逐一定性(SPEF 标注报告在档)。
- 门级仿真未执行(SDF 已传 EDAServer 备用,属后续工作)。
- 时间线:Stage F+G+H 全程约 2.5 小时,预算 6 小时内。

——以上每项数字均出自归档的原始报告/日志(路径与 sha256 见
`flow/local_runs/icc2_28nm_20260807/signoff/MANIFEST.md`),无推断值。
